{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx -y serve public -l 3000"
  },
  "deploy": {
    "startCommand": "npx -y serve public -l 3000",
    "numReplicas": 1
  }
}
